name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write # Crucial for creating releases

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: lesson-cap/go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.12.0
      - name: Build macOS
        run: |
          cd lesson-cap
          npm ci --prefix frontend
          wails build -platform darwin/universal -clean
      - name: Sign and Package
        run: |
          cd lesson-cap/build/bin
          codesign --force --deep --sign - LessonCap.app
          zip -r LessonCap_macOS.zip LessonCap.app
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: lesson-cap/build/bin/LessonCap_macOS.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: lesson-cap/go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.12.0
      - name: Build Windows
        run: |
          cd lesson-cap
          npm ci --prefix frontend
          wails build -platform windows/amd64 -clean
      - name: Package
        run: |
          cd lesson-cap/build/bin
          Compress-Archive -Path LessonCap.exe -DestinationPath LessonCap_Windows.zip
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: lesson-cap/build/bin/LessonCap_Windows.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
